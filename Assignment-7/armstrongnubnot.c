#include<stdio.h>
#include<math.h>
int main()
{
	int = 15 ,count = 0, sum = 0;
	int n = x;
	while(n !=0)
	
		n = n/10;
		count++;
	
	
	n = x;
	while(n !=0)
	
		sum = sum + pow(n%10,count);
		n = n/10;
	
	if(x ==sum)
	  printf("yes\n\n\n");
	else
	  printf("no\n\n\n");
	return 0;
}
